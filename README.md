# Samba_Linux_Prueba
Contenedor de Samba para un linux server virtualizado y asi poder verlo en mi misma compu

Los archivos docker-compose_1.yml y docker-compose_v.yml son ejemplos de como seria para uno o varioas usuarios con diferentes contraseñas, pero solo se deberian de llama docker-compose.yml

docker-compose_1.yml - ejemplo con 1 solo usuario

docker-compose_v.yml - ejemplo con varios usuarios
